import { Size } from './types'

export const labelSizeVariants: Record<Size, string> = {
  sm: 'text-sm',
  md: 'text-md',
  lg: 'text-lg',
}

export const placeholderSizeVariants: Record<Size, string> = {
  sm: 'text-sm',
  md: 'text-base',
  lg: 'text-lg',
}
